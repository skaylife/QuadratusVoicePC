
# 🛠 Инструкция по сборке QuadratusVoicePC

Этот документ содержит все необходимые команды для компиляции приложения **Quadratus Voice AI** в различных форматах.

## 📌 Подготовка
Перед запуском команд убедитесь, что:
1. Вы находитесь в папке проекта.
2. Виртуальное окружение активировано: `.\env\Scripts\activate` (или `(env)` отображается в терминале).
3. Установлены все зависимости: `pip install -r requirements.txt`.
4. В папке проекта присутствуют файлы: `icon.ico`, `file_version_info.txt`, `QuadratusVoicePC.py` (ранее `test9.py`).

---

## 🚀 Вариант 1: Сборка в папку (для Инсталлера)
**Рекомендуемый вариант.** Обеспечивает мгновенный запуск программы, так как файлы не требуют распаковки во временную директорию. Идеально подходит для создания установщика через Inno Setup.

```powershell
python -m PyInstaller --noconsole --uac-admin --clean --collect-all vosk --paths=env/Lib/site-packages --icon="icon.ico" --version-file="file_version_info.txt" --name="QuadratusVoicePC" QuadratusVoicePC.py
```

*   **Результат:** Папка `dist/QuadratusVoicePC/`.
*   **Плюс:** Мгновенный запуск приложения.
*   **Минус:** Много файлов в папке (нужно упаковывать в Setup.exe).

---

## 📦 Вариант 2: Сборка в ОДИН файл (Portable)
Создает один исполняемый файл, который удобно пересылать. При запуске он автоматически распаковывает библиотеки во временную папку.

```powershell
python -m PyInstaller --noconsole --onefile --uac-admin --clean --collect-all vosk --paths=env/Lib/site-packages --icon="icon.ico" --version-file="file_version_info.txt" --name="QuadratusVoicePC_Portable" QuadratusVoicePC.py
```

*   **Результат:** Файл `dist/QuadratusVoicePC_Portable.exe`.
*   **Плюс:** Один переносимый файл.
*   **Минус:** Запуск занимает 5-10 секунд (распаковка Vosk).

---

## 🔍 Описание параметров командной строки

| Флаг | Описание |
| :--- | :--- |
| `--noconsole` | Отключает появление черного окна терминала при запуске программы. |
| `--onefile` | Собирает всё приложение в один `.exe` файл. |
| `--uac-admin` | Запрашивает права администратора при запуске (необходимо для работы библиотеки `keyboard`). |
| `--clean` | Очищает кэш PyInstaller перед сборкой (предотвращает ошибки старых версий). |
| `--collect-all vosk` | Принудительно упаковывает все внутренние зависимости нейросети Vosk (DLL, метаданные). |
| `--paths=env/Lib/site-packages` | Указывает путь к библиотекам внутри виртуального окружения. |
| `--icon` | Устанавливает иконку приложения. |
| `--version-file` | Вшивает метаданные (версия, автор, сайт, русский язык). |
| `--name` | Задает имя итогового исполняемого файла. |

---

## 🛡 Возможные проблемы

1.  **Ошибка "Permission Denied":** Убедитесь, что прошлая версия программы закрыта (проверьте в диспетчере задач).
2.  **Антивирус:** Windows Defender может блокировать сборку `--onefile`. Рекомендуется временно отключить защиту или добавить папку проекта в исключения.
3.  **Vosk не найден:** Если после сборки ошибка "No module named vosk", проверьте, что флаг `--collect-all vosk` прописан правильно и вы запускаете сборку через `python -m PyInstaller`.

---

## 🛠 Создание инсталлера (QuadratusVoicePC_Setup.exe)
1. Соберите проект по **Варианту 1** (папка).
2. Откройте **Inno Setup Compiler**.
3. Создайте новый скрипт через Wizard.
4. Выберите `dist/QuadratusVoicePC/QuadratusVoicePC.exe` как главный файл.
5. Добавьте всю папку `dist/QuadratusVoicePC/` в список дополнительных файлов.
6. Скомпилируйте Setup.

---
**Quadratus Software © 2026** | [q-voice.netlify.app](https://q-voice.netlify.app/)